#!/usr/bin/env bash

MAIN='eDP-1'
EXTERNAL='HDMI-1'

if output=$(xrandr | grep "$EXTERNAL connected"); then
    xrandr --output $EXTERNAL --auto
    xrandr --output $MAIN --off
else
    xrandr --output $MAIN --auto
fi
