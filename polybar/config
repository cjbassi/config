[colors]
; ;background = ${xrdb:color0:#222}
; background = #222
; background-alt = #444
; ;foreground = ${xrdb:color7:#222}
; foreground = #dfdfdf
; foreground-alt = #555
; primary = #ffb52a
; secondary = #e60053
; alert = #bd2c40
; background =    #131313
; foreground =    #b2b2b2

white =         #FFFFFF
dark_grey =     #1a1a1a
light_grey =    #292b2e
blue =          #0050FF
black =         #000000
red =           #ee3333
green =         #20ad80

background =    ${colors.dark_grey}
foreground =    ${colors.white}

[bar/main]
;monitor = ${env:MONITOR:HDMI-1}
width = 100%
height = 25
;offset-x = 1%
;offset-y = 1%
radius = 1.0
fixed-center = true
bottom = true

background =    ${colors.light_grey}
foreground =    ${colors.foreground}

; line-size = 3
; line-color = #f00

; border-size = 0
; border-color = #00000000

; padding-left = 0
; padding-right = 2

; module-margin-left = 1
; module-margin-right = 0

font-0 = fixed:pixelsize=12;3
font-1 = unifont:fontformat=truetype:size=8:antialias=false;3
font-3 = siji:pixelsize=10;3
font-2 = Inconsolata for Powerline:pixelsize=20;4

modules-left = i3 left_arrow1 i3index left_arrow2
modules-center =
; modules-right = memory arrow1 cpu arrow1 day arrow1 date arrow1 time
modules-right = right_arrow1 ram right_arrow2 cpu right_arrow1 battery right_arrow2 volume right_arrow1 day right_arrow2 date right_arrow1 time right_arrow2

tray-position = right
; tray-padding = 4
;tray-transparent = true
;tray-background = #0063ff

;wm-restack = bspwm
;wm-restack = i3

;override-redirect = true

;scroll-up = bspwm-desknext
;scroll-down = bspwm-deskprev

;scroll-up = i3wm-wsnext
;scroll-down = i3wm-wsprev

[module/left_arrow1]
type = custom/text
content = " "
content-font = 2
content-foreground = ${colors.dark_grey}
content-background = ${colors.blue}

[module/left_arrow2]
type = custom/text
content = " "
content-font = 2
content-foreground = ${colors.blue}
content-background = ${colors.light_grey}

[module/right_arrow1]
type = custom/text
content = " "
content-font = 2
content-foreground = ${colors.dark_grey}
content-background = ${colors.light_grey}

[module/right_arrow2]
type = custom/text
content = " "
content-font = 2
content-foreground = ${colors.light_grey}
content-background = ${colors.dark_grey}

[module/my_filesystem]
type = custom/script
interval = 30
label = %{F#0a81f5}/%{F-} : %output%B
exec = ~/config/filesystem.sh
format-prefix = " "
format-underline = #006400

[module/xwindow]
type = internal/xwindow
label = %title:0:30:...%

[module/xkeyboard]
type = internal/xkeyboard
blacklist-0 = num lock

format-prefix = " "
format-prefix-foreground = ${colors.foreground-alt}
format-prefix-underline = ${colors.secondary}

label-layout = %layout%
label-layout-underline = ${colors.secondary}

label-indicator-padding = 2
label-indicator-margin = 1
label-indicator-background = ${colors.secondary}
label-indicator-underline = ${colors.secondary}

[module/battery]
type = internal/battery
battery = BAT0
adapter = AC
full-at = 98

format-charging = <animation-charging> <label-charging>
format-charging-foreground = ${colors.green}
format-charging-background = ${colors.dark_grey}
format-discharging = <ramp-capacity> <label-discharging>
format-discharging-foreground = ${colors.green}
format-discharging-background = ${colors.dark_grey}
format-full = <label-full>
format-full-foreground = ${colors.green}
format-full-background = ${colors.dark_grey}

label-charging = "CHR %percentage%%"
label-discharging = "BAT %percentage%%"
label-discharging-foreground = ${colors.green}
label-full = "BAT %percentage%%"

ramp-capacity-0 =
ramp-capacity-0-foreground = ${colors.red}
ramp-capacity-1 =
ramp-capacity-1-foreground = ${colors.red}
ramp-capacity-2 =
ramp-capacity-3 =
ramp-capacity-4 =
animation-charging-0 =
animation-charging-1 =
animation-charging-2 =
animation-charging-3 =
animation-charging-4 =
animation-charging-framerate = 750

[module/filesystem]
type = internal/fs
interval = 30

mount-0 = /

format-underline = #f90000
label-mounted = %{F#0a81f5}%mountpoint%%{F-} : %free%

; [module/i3]
; type = internal/i3
; format = <label-state> <label-mode>
; index-sort = true
; wrapping-scroll = false
; enable-click = true

; ; Only show workspaces on the same output as the bar
; ;pin-workspaces = true

; label-mode-padding = 2
; label-mode-foreground = #000
; label-mode-background = ${colors.primary}

; ; focused = Active workspace on focused monitor
; label-focused = %name%
; label-focused-background = ${module/bspwm.label-focused-background}
; label-focused-underline = ${module/bspwm.label-focused-underline}
; label-focused-padding = ${module/bspwm.label-focused-padding}

; ; unfocused = Inactive workspace on any monitor
; label-unfocused = %name%
; label-unfocused-padding = ${module/bspwm.label-occupied-padding}

; ; visible = Active workspace on unfocused monitor
; label-visible = %name%
; label-visible-background = ${self.label-focused-background}
; label-visible-underline = ${self.label-focused-underline}
; label-visible-padding = ${self.label-focused-padding}

; ; urgent = Workspace with urgency hint set
; label-urgent = %name%
; label-urgent-background = ${module/bspwm.label-urgent-background}
; label-urgent-padding = ${module/bspwm.label-urgent-padding}

[module/i3]
type = internal/i3
format = <label-state><label-mode>

index-sort = true
enable-click = false
enable-scroll = false

ws-icon-0 = 0;*
ws-icon-default = ○

; Only show workspaces on the same output as the bar
pin-workspaces = true

label-mode = " %mode% "
label-mode-foreground = ${colors.red}
label-mode-background = ${colors.background}

; focused = Active workspace on focused monitor
label-focused = " ● "
label-focused-foreground = ${colors.blue}
label-focused-background = ${colors.background}

; unfocused = Inactive workspace on any monitor
label-unfocused = " %icon% "
label-unfocused-foreground = ${colors.foreground}
label-unfocused-background = ${colors.background}

; urgent = Workspace with urgency hint set
label-urgent = " ● "
label-urgent-foreground = ${colors.red}
label-urgent-background = ${colors.background}

[module/i3index]
type = internal/i3
format = <label-state>

label-focused = "%name% "
label-focused-foreground = ${colors.dark_grey}
label-focused-background = ${colors.blue}

label-unfocused =
label-urgent =

[module/mpd]
type = internal/mpd
format-online = <label-song>  <icon-prev> <icon-stop> <toggle> <icon-next>

icon-prev = 
icon-stop = 
icon-play = 
icon-pause = 
icon-next = 

label-song-maxlen = 25
label-song-ellipsis = true

[module/xbacklight]
type = internal/xbacklight

format = <label> <bar>
label = BL

bar-width = 10
bar-indicator = |
bar-indicator-foreground = #ff
bar-indicator-font = 2
bar-fill = ─
bar-fill-font = 2
bar-fill-foreground = #9f78e1
bar-empty = ─
bar-empty-font = 2
bar-empty-foreground = ${colors.foreground-alt}

[module/backlight-acpi]
inherit = module/xbacklight
type = internal/backlight
card = intel_backlight

[module/cpu]
type = internal/cpu
interval = 1
format-prefix = " CPU "
format-prefix-foreground = ${colors.white}
format-prefix-background = ${colors.light_grey}
label = %percentage%%
label-foreground = ${colors.white}
label-background = ${colors.light_grey}

[module/ram]
type = internal/memory
interval = 1
format-prefix = " RAM "
format-prefix-foreground = ${colors.white}
format-prefix-background = ${colors.dark_grey}
label = %percentage_used%%
label-foreground = ${colors.white}
label-background = ${colors.dark_grey}

[module/wlan]
type = internal/network
interface = net1
interval = 1

format-connected = <ramp-signal> <label-connected>
format-connected-underline = #9f78e1
label-connected = %essid%

format-disconnected =
;format-disconnected = <label-disconnected>
;format-disconnected-underline = ${self.format-connected-underline}
;label-disconnected = %ifname% disconnected
;label-disconnected-foreground = ${colors.foreground-alt}

ramp-signal-0 = 
ramp-signal-1 = 
ramp-signal-2 = 
ramp-signal-3 = 
ramp-signal-4 = 
ramp-signal-foreground = ${colors.foreground-alt}

[module/eth]
type = internal/network
interface = enp4s0
interval = 1

format-connected-underline = #55aa55
; format-connected-prefix = " "
format-connected-prefix-foreground = ${colors.foreground-alt}
label-connected = Up: %upspeed:7% Down: %downspeed:7%
;   %ifname%    [wireless+wired]
;   %local_ip%  [wireless+wired]
;   %essid%     [wireless]
;   %signal%    [wireless]
;   %upspeed%   [wireless+wired]
;   %downspeed% [wireless+wired]
;   %linkspeed% [wired]

format-disconnected =
;format-disconnected = <label-disconnected>
;format-disconnected-underline = ${self.format-connected-underline}
;label-disconnected = %ifname% disconnected
;label-disconnected-foreground = ${colors.foreground-alt}

[module/date]
type = internal/date
interval = 1

date = " %B %d"

label-foreground = ${colors.white}
label-background = ${colors.light_grey}

label = %date%

[module/day]
type = internal/date
interval = 1

date = " %A"

label = "%date%"
label-foreground = ${colors.white}
label-background = ${colors.dark_grey}

[module/time]
type = internal/date
interval = 1

time = " %H:%M"

label-foreground = ${colors.white}
label-background = ${colors.dark_grey}

label = %time%

[module/volume]
type = internal/volume

; mapping = true
format-volume = <ramp-volume><label-volume>

label-volume = %percentage%%
label-volume-foreground = ${colors.blue}
label-volume-background = ${colors.light_grey}
label-muted = " MUTED "
label-muted-foreground = ${colors.blue}
label-muted-background = ${colors.light_grey}

ramp-volume-0 = " VOL "
ramp-volume-0-foreground = ${colors.blue}
ramp-volume-0-background = ${colors.light_grey}
ramp-volume-1 = " VOL "
ramp-volume-1-foreground = ${colors.blue}
ramp-volume-1-background = ${colors.light_grey}
ramp-volume-2 = " VOL "
ramp-volume-2-foreground = ${colors.blue}
ramp-volume-2-background = ${colors.light_grey}
ramp-volume-3 = " VOL "
ramp-volume-3-foreground = ${colors.blue}
ramp-volume-3-background = ${colors.light_grey}

; [module/volume]
; type = custom/script
; interval = 1
; label = %output%
; ; label-padding = 1
; exec = ~/config/polybar/volume.sh
; format-underline = #ffff00

; type = internal/volume

; format-volume = <label-volume><bar-volume>

; label-volume = VOL
; label-volume-foreground = ${root.foreground}

; label-muted = sound muted
; format-muted-foreground = ${colors.foreground-alt}

; Soundcard to be used
; master-soundcard = default
; speaker-soundcard = default
; headphone-soundcard = default

; Name of the master mixer
; master-mixer = Master

; Optionally define speaker and headphone mixers
; Use the following command to list available mixer controls:
; $ amixer scontrols | sed -nr "s/.*'([[:alnum:]]+)'.*/\1/p"
; Default: none
; speaker-mixer = Speaker
; Default: none
; headphone-mixer = Headphone

; NOTE: This is required if headphone_mixer is defined
; Use the following command to list available device controls
; $ amixer controls | sed -r "/CARD/\!d; s/.*=([0-9]+).*name='([^']+)'.*/printf '%3.0f: %s\n' '\1' '\2'/e" | sort
; Default: none
; headphone-id = 9

; Use volume mapping (similar to amixer -M and alsamixer), where the increase in volume is linear to the ear
; Default: false
; mapped = true

; format-muted-prefix = " "

; bar-volume-width = 10
; bar-volume-foreground-0 = #55aa55
; bar-volume-foreground-1 = #55aa55
; bar-volume-foreground-2 = #55aa55
; bar-volume-foreground-3 = #55aa55
; bar-volume-foreground-4 = #55aa55
; bar-volume-foreground-5 = #f5a70a
; bar-volume-foreground-6 = #ff5555
; bar-volume-gradient = false
; bar-volume-indicator = |
; bar-volume-indicator-font = 2
; bar-volume-fill = ─
; bar-volume-fill-font = 2
; bar-volume-empty = ─
; bar-volume-empty-font = 2
; bar-volume-empty-foreground = ${colors.foreground-alt}

; [module/battery]
; type = internal/battery
; battery = BAT0
; adapter = ADP1
; full-at = 98

; format-charging = <animation-charging> <label-charging>

; format-discharging = <ramp-capacity> <label-discharging>

; format-full-prefix = " "
; format-full-prefix-foreground = ${colors.light_grey}

; ramp-capacity-0 = 
; ramp-capacity-1 = 
; ramp-capacity-2 = 
; ramp-capacity-foreground = ${colors.light_grey}

; animation-charging-0 = 
; animation-charging-1 = 
; animation-charging-2 = 
; animation-charging-foreground = ${colors.light_grey}
; animation-charging-framerate = 750

[module/powermenu]
type = custom/menu

format-spacing = 1

label-open = 
label-open-foreground = ${colors.secondary}
label-close =  cancel
label-close-foreground = ${colors.secondary}
label-separator = |
label-separator-foreground = ${colors.foreground-alt}

menu-0-0 = reboot
menu-0-0-exec = menu-open-1
menu-0-1 = power off
menu-0-1-exec = menu-open-2

menu-1-0 = cancel
menu-1-0-exec = menu-open-0
menu-1-1 = reboot
menu-1-1-exec = sudo reboot

menu-2-0 = power off
menu-2-0-exec = sudo poweroff
menu-2-1 = cancel
menu-2-1-exec = menu-open-0

[settings]
screenchange-reload = true
;compositing-background = xor
;compositing-background = screen
;compositing-foreground = source
;compositing-border = over

[global/wm]
margin-top = 5
margin-bottom = 5

; vim:ft=dosini
